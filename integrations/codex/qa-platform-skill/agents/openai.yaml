interface:
  display_name: "QA Platform Scanner"
  short_description: "扫描 API、推导目录并生成模块化导入包"
  default_prompt: "Use $qa-platform-skill to map configured service topology into external route prefixes and service/business API directories, deterministically convert OpenAPI/Swagger and Java source contracts into complete executable definitions with JavaDoc/annotation descriptions, recursive request fields, and response schemas, apply configured API directory rules, discover safe reusable API templates, scan remaining HTTP/WS routes, read configured flow documents, validate the module directory, and build a versioned qa-platform ZIP."
