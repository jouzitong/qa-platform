interface:
  display_name: "QA Platform Scanner"
  short_description: "扫描接口并生成待审核流程与版本计划"
  default_prompt: "Use $qa-platform-skill to scan all HTTP/WS interfaces, assign business keys, detect the gateway, and generate a validated versioned qa-platform import package with draft flows and plans."
